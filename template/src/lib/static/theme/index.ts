import { PRIMARY, SECONDARY } from '../colors';

const theme = {
    mode: 'light',
    palette: {
        primary: {
            main: PRIMARY,
        },
        secondary: {
            main: SECONDARY,
        },
        background: {
            default: '#fff !important',
        },
    },
};

export default theme;
